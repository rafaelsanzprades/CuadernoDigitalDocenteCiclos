# Generar con: python -c "import secrets; print(secrets.token_urlsafe(32))"
CDD_SECRET_KEY=

# Opcional: orígenes CORS permitidos (coma-separados, sin espacios)
# Por defecto: http://localhost:3000,http://127.0.0.1:3000
# CORS_ORIGINS=https://tudominio.com

# URL de base de datos (SQLite por defecto, PostgreSQL en producción)
# DATABASE_URL=postgresql://user:pass@host:5432/cdd_pro
